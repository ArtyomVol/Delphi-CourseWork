program DecomposeTimeProcess;

uses
  Vcl.Forms,
  SysUtils,
  UnitMain in 'UnitMain.pas' {Form1},
  DecomposingTimeProcess in 'DecomposingTimeProcess.pas',
  ComboBoxActions in 'ComboBoxActions.pas',
  UnitTableView in 'UnitTableView.pas' {Form2},
  UnitColorChange in 'UnitColorChange.pas' {Form3},
  UnitAbout in 'UnitAbout.pas' {Form4};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm3, Form3);
  Application.CreateForm(TForm4, Form4);
  Application.Run;
end.
